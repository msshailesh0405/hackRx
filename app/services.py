def process_questions(documents: str, questions: list) -> list:
    """
    Placeholder logic for answering questions.
    In later rounds, replace with LLM + vector DB retrieval.
    """
    answers = []
    for q in questions:
        answers.append(f"Based on the provided documents, here is a mock answer to: '{q}'")
    return answers
